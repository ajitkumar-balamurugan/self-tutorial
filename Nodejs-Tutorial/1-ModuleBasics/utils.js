const sayHi = (name) => {
  console.log(`Hello ${name}`);
};

const items = (names) => {
  console.log(`Get lost ${names}`);
};
module.exports.sayHi = sayHi;
module.exports.items = items;
// console.log(module);
